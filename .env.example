# Locize Backup Configuration Example
# This project now uses split environment files for better organization:
# - Copy .env.local for common Locize and backup settings
# - Copy .env.s3 for AWS S3 configuration (production)
# - Copy .env.minio for MinIO configuration (testing/development)
#
# Usage:
# For S3: source .env.local && source .env.s3
# For MinIO: source .env.local && source .env.minio

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Locize Project ID (REQUIRED)
LOCIZE_PROJECT_ID=your-project-id-here

# =============================================================================
# LOCIZE CLI CONFIGURATION
# =============================================================================

# Locize API Key (OPTIONAL - required for private projects)
# Leave empty for public projects
LOCIZE_API_KEY=

# Project version/branch to backup (default: latest)
LOCIZE_VERSION=latest

# Languages to backup (comma-separated)
LOCIZE_LANGUAGES=en,fr,de,ja,ko,zh

# Namespaces to backup (comma-separated)
LOCIZE_NAMESPACES=frontend,backend-templates,configurations-schemes,configurations-forms

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================

# S3 Bucket for storing backups (OPTIONAL - if not set, backups will be stored locally)
# Leave empty to store backups only in local directory
S3_BUCKET_NAME=

# AWS Region for S3 bucket (only required if S3_BUCKET_NAME is set)
AWS_REGION=us-east-1



# =============================================================================
# AWS CREDENTIALS (Required when using S3)
# =============================================================================

# AWS credentials for S3 access (only required if S3_BUCKET_NAME is set)
# If not using IAM roles, set these environment variables:
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Optional: AWS session token for temporary credentials
AWS_SESSION_TOKEN=

# Optional: AWS profile name (alternative to access key/secret)
AWS_PROFILE=

# =============================================================================
# BACKUP BEHAVIOR
# =============================================================================

# Maximum number of retry attempts for failed operations
MAX_RETRIES=3

# Delay between retry attempts (seconds)
RETRY_DELAY=5

# Delay between API calls to avoid rate limiting (seconds)
RATE_LIMIT_DELAY=1

# Whether to cleanup local files after successful S3 upload
CLEANUP_LOCAL_FILES=true

# Timeout for locize-cli commands (seconds)
LOCIZE_CLI_TIMEOUT=30

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# =============================================================================
# KUBERNETES SPECIFIC (when running in K8s)
# =============================================================================

# Pod name for logging (automatically set in K8s)
# POD_NAME=

# Namespace for logging (automatically set in K8s)
# POD_NAMESPACE=